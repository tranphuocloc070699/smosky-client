const prefix = '/api'

const Routes = {
    BOILERPLATE: {
        SPRING:`${prefix}/spring`
    },
    POST:`${prefix}/posts`,
    REVIEW:`${prefix}/reviews`,
    ADMIN:`${prefix}/admin`
}

export default Routes 