export default defineAppConfig({
    ui: {
        primary: 'emerald',
        notifications:{
            position:'top-0 right-[50%] transform translate-x-[50%] bottom-auto'
        },
        notification:{
                progress:{
                    base:'hidden'
                }
        }
    }
  })